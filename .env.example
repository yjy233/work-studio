# Export these variables in your terminal before starting the app.
# Optional; by default the Codex executable on PATH is used.
# CODEX_BIN=/path/to/codex
# Optional; otherwise Codex uses your configured default model.
# CODEX_MODEL=your-model
# PORT=4310
# WIKI_DIR=/absolute/path/to/wiki
